import React from 'react'

import clases from '../../../components/Navigation/Toolbar/Toolbar.css'
import Logo from '../../../components/Logo/Logo'
const toolbar = (props)=>(
    <header className="Toolbar">
        <div>Menü</div>
        <div><Logo/></div>
        <nav>
            ...
        </nav>
    </header>
);
export  default  toolbar
